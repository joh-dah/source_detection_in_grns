data_creation:
  network: 10716_not_connected_100
  normalize_data: true
  remove_near_duplicates: false

graph_perturbation:
  noise:
    missing_edges: 0
    wrong_edges: 0

training:
  epochs: 200
  batch_size: 8 

gene_specific_metrics:
  enabled: false  # Deaktiviert da keine raw data
