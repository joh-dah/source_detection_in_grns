data_creation:
  n_samples: 1000 
  time_steps: [1]
  network: custom_10

graph_perturbation: 
  noise:
    missing_edges: 0
    wrong_edges: 0

training:
  epochs: 5

# Gene-specific metrics configuration example
gene_specific_metrics:
  enabled: true
  genes_of_interest: ["A", "B", "C", "D", "E"]  # Example genes of interest
