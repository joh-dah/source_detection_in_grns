model: PDGrapher
model_name: pdgrapher_default

training:
  epochs: 50
  layers: 2
  alpha: 0.5
  batch_size: 64
  class_weighting: false
  graph_weighting: false
  hidden_size: 128
  heads: 4
  dropout: 0.3
  learning_rate: 1.0e-4
  subsample: false
  weight_decay: 5.0e-4
